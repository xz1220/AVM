# Testdata

Fixtures in this directory should be deterministic and scoped to a single package
or integration scenario.

Shared Phase 1 golden fixtures live under `fixtures/phase1/`. Package tests may
mirror or reference them from `testdata/phase1/` when they need package-local
ownership.
